import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'svit.duckdns.org',
  appName: 'svit',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
