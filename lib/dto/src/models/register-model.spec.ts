import { expect } from 'chai'
import { RegisterModel } from './register-model'

describe('RegisterModel', () => {
    it('should create an instance', () => {
        expect(new RegisterModel()).to.be.true
    })
})
